# Optional private account/connection service. Without it, ImageStitch keeps a
# browser-bound device profile and leaves cloud/provider actions unavailable.
VITE_IMAGESTITCH_ACCOUNT_API_URL=https://account.imagestitch.example
